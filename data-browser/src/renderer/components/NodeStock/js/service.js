const service = {
    config: null,
    init(config) {
        this.config = config;
    },
    loadData() {
        return new Promise((resole, reject) => {

        });
    }
};

export default service;